apiVersion: v1
kind: Secret
metadata:
  name: push-credentials
  annotations:
    build.knative.dev/docker-0: https://gcr.io
type: kubernetes.io/basic-auth
stringData:
  username: _json_key
  # The content of the JSON key file for the serviceaccount credentials (including the curly braces) should replace the placeholder below
  password: |
    {JSON key file content}
