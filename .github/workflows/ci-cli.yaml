name: CI - cli

on:
  push:
    branches:
    - '**'
    - '!dependabot/**'
  pull_request: {}

jobs:

  unit:
    name: Unit test
    strategy:
      matrix:
        os:
        - ubuntu-latest
        - macos-latest
        - windows-latest
      fail-fast: false
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v1
      with:
        go-version: '1.13'
    # TODO remove after https://github.com/actions/setup-go/issues/14
    - name: Add GOPATH/bin to PATH
      run: |
        echo "##[set-env name=GOPATH;]$(go env GOPATH)"
        echo "##[add-path]$(go env GOPATH)/bin"
      working-directory: cli
      shell: bash
    - name: Verify code formatting
      run: |
        go get golang.org/x/tools/cmd/goimports
        make verify-goimports
      shell: bash
      working-directory: cli
      if: success() && runner.os != 'Windows'
    - name: Build
      run: make build
      working-directory: cli
    - name: Unit test
      run: make coverage
      working-directory: cli
    - name: Verify docs
      run: make verify-docs
      working-directory: cli
    - name: Report coverage
      run: curl -s https://codecov.io/bash | bash -s
      shell: bash
      working-directory: cli
      env:
        CODECOV_TOKEN: d09bee7e-6c5a-4316-a48f-266e08d0f70f

  stage:
    name: Stage
    needs: unit
    if: success() && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v1
      with:
        go-version: '1.13'
    # TODO remove after https://github.com/actions/setup-go/issues/14
    - name: Add GOPATH/bin to PATH
      run: |
        echo "##[set-env name=GOPATH]$(go env GOPATH)"
        echo "##[add-path]$(go env GOPATH)/bin"
      working-directory: cli
    - name: Stage riff release artifacts
      run: .github/workflows/ci-cli/stage.sh
      working-directory: cli
      env:
        GCLOUD_CLIENT_SECRET: ${{ secrets.GCLOUD_CLIENT_SECRET }}

  acceptance:
    name: Acceptance
    needs: stage
    if: success() && github.event_name == 'push'
    strategy:
      matrix:
        config:
        - os: ubuntu-latest
          qualifier: kind
          cluster: kind
          registry: dockerhub
        - os: windows-latest
          qualifier: windows
          cluster: gke
          registry: gcr
      fail-fast: false
    runs-on: ${{ matrix.config.os }}
    env:
      CLUSTER: ${{ matrix.config.cluster }}
      REGISTRY: ${{ matrix.config.registry }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      GCLOUD_CLIENT_SECRET: ${{ secrets.GCLOUD_CLIENT_SECRET }}
    steps:
      - uses: actions/checkout@v2
      - name: Setup env
        run: |
          job=$(date +%s) # TODO use something that is assigned by CI to guarantee uniqueness
          echo "JOB_ID=${job}"

          echo "##[set-env name=CLUSTER_NAME]cli-${job}-${{ matrix.config.qualifier }}"
          echo "##[set-env name=NAMESPACE]cli-${job}-${{ matrix.config.qualifier }}"
        working-directory: cli
        shell: bash
      - name: Run acceptance tests
        run: .github/workflows/ci-cli/acceptance.sh
        working-directory: cli
        shell: bash
      - name: Collect diagnostics
        run: ./fats/diagnostics.sh
        working-directory: cli
        shell: bash
        if: always()
      - name: Cleanup
        run: .github/workflows/ci-cli/acceptance-cleanup.sh
        working-directory: cli
        shell: bash
        if: always()

  publish:
    name: Publish
    needs: acceptance
    if: |
      success() && 
      github.event_name == 'push' && (
        github.ref == 'refs/heads/master' || (
          startsWith(github.ref, 'refs/heads/v') && endsWith(github.ref, 'x')
        )
      )
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Publish riff release artifacts
      run: .github/workflows/ci-cli/publish.sh ${{ github.ref }}
      working-directory: cli
      env:
        GCLOUD_CLIENT_SECRET: ${{ secrets.GCLOUD_CLIENT_SECRET }}
